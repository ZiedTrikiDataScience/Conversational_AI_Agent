version: 1
formatters:
  detailed:
    format: "[%(asctime)s] %(levelname)s %(name)s: %(message)s"
handlers:
  console:
    class: logging.StreamHandler
    formatter: detailed
    level: INFO
loggers:
  ai_agent:
    level: INFO
    handlers: [console]
    propagate: no
